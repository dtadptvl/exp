# Kilo Prime/Sub researched package

This branch stores the tested standalone package as an exact ZIP snapshot.

- Artifact: `kilo-prime-sub-researched.zip`
- SHA-256: `4face500c917c52e29a9f028a82fd2edb89ba79c7a727aadb4c1faf0ac4dadc2`
- Local static validation: 14/14 tests passed before this archive was created.
- Reference ZIP static validation: 9/9 tests passed.
- Live Prime-only / baseline / optimized model evaluation: `NOT_RUN` in the authoring environment because no `kilo` executable or user provider credentials were available.

The archive contains the complete standalone folder: global `prime.md` and `Sub.md`, installer/uninstaller, config merge guide, minimal state and task-contract protocols, smoke test, 20-case evaluation set, rubric, harness, research report, and rejected-machinery review.

The package deliberately does not claim empirical success until the live three-way benchmark is run with the intended Prime model and `9router/sub`.
